# Railway Environment Variables for Mamo Compounder Docker Deployment

# Base network RPC URL (from wrangler.jsonc)
BASE_RPC_URL=https://rpc.moonwell.fi/main/evm/8453

# Minimum USD value threshold for claiming rewards (from wrangler.jsonc)
MIN_USD_VALUE_THRESHOLD=5

# Private key for transaction signing (must be set in Railway dashboard)
# PRIVATE_KEY=your_private_key_here

# Cron schedule for running the compounder job (every 15 minutes)
CRON_SCHEDULE=*/15 * * * *

# Port for the web server (Railway will set this automatically)
PORT=3000

# Set to production for optimized performance
NODE_ENV=production